<Window x:Class="InstaChat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="InstaChat"
        Width="1080" Height="760"
        MinWidth="640" MinHeight="480"
        WindowStartupLocation="CenterScreen"
        Background="#FAFAFA">
    <Window.InputBindings>
        <KeyBinding Modifiers="Control" Key="D" Command="{Binding OpenInboxCommand}" />
        <KeyBinding Modifiers="Control" Key="N" Command="{Binding NewMessageCommand}" />
        <KeyBinding Modifiers="Shift" Key="L" Command="{Binding LogoutCommand}" />
    </Window.InputBindings>
    <DockPanel>
        <Menu DockPanel.Dock="Top" Padding="6,3" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <MenuItem Header="_Messages">
                <MenuItem Header="_Open inbox" InputGestureText="Ctrl+D" Command="{Binding OpenInboxCommand}" />
                <MenuItem Header="New _message" InputGestureText="Ctrl+N" Command="{Binding NewMessageCommand}" />
                <MenuItem Header="_Reload" InputGestureText="F5" Command="{Binding ReloadCommand}" />
                <Separator />
                <MenuItem Header="E_xit" Command="{Binding ExitCommand}" />
            </MenuItem>
            <MenuItem Header="_Privacy">
                <MenuItem Header="Log out &amp; clear local data…" InputGestureText="Ctrl+Shift+L" Command="{Binding LogoutCommand}" />
                <Separator />
                <MenuItem Header="_About InstaChat" Command="{Binding AboutCommand}" />
            </MenuItem>
        </Menu>
        <Grid>
            <wv2:WebView2 x:Name="Web" />
            <Grid x:Name="OfflineOverlay" Visibility="Collapsed" Background="#F2F7F7F9">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Can't reach Instagram"
                               FontSize="20" FontWeight="SemiBold"
                               HorizontalAlignment="Center" />
                    <TextBlock x:Name="OfflineDetail"
                               Text="Check your internet connection and try again."
                               Foreground="#666666" Margin="0,8,0,20"
                               HorizontalAlignment="Center" />
                    <Button Content="Retry"
                            Padding="32,6"
                            HorizontalAlignment="Center"
                            Command="{Binding RetryCommand}" />
                </StackPanel>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
